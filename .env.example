# Pick one — only one needs to be set
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: which judge model to use
JUDGE_MODEL=claude-sonnet-4-5
# Optional: which pipeline model is being evaluated (informational only)
PIPELINE_MODEL=gpt-4o-mini
